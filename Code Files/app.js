var queueController = (function() {
    
    return {
        // 1. Get input from fields
        getInput: function(){
            
        },
        // 2. create queue object from values
        
        // 3. push queue object to end of queue object array
        
    }
})();

var UIController = (function() {
    
})();

var AppController = (function(qCtrl, UICtrl) {
    var init = function(){
        document.querySelector('.submitEntry').addEventListener('click', addItem);
        
    //// addItem function
        
        // getInput
        
        // create object
        
        // add object to array
        
        // create new table row UICtrl
        
        // populate table row with object values
        
        
    }
})(queueController, UIController);

AppController.init();